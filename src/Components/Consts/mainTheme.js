import { createTheme } from "@mui/material/styles";

export const mainTheme = createTheme({
    palette: {
      mode: 'dark',
      primary: {
        main: '#191414',
      },
      secondary: {
        main: '#1DB954',
      },
    },
  });
